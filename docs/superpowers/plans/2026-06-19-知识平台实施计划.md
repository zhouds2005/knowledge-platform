# 知识平台 Phase 1 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成 MaxKey 认证集成、存储/预览/搜索/通知/版本控制功能补齐、UI 重新设计、内网部署，让 10-50 人实际使用。

**Architecture:** 保持现有 React 19 + Express 5 + PostgreSQL + Nextcloud 架构。认证从自建 session-cookie 换成 MaxKey OIDC；权限新增空间管理员角色；存储以 Nextcloud 为核心；预览引入 LibreOffice；版本控制补齐追踪和回滚；UI 全面换皮（琥珀金配深石板灰+思源字体）。

**Tech Stack:** React 19, Vite 7, TypeScript, Tailwind CSS 4, Tiptap, Express 5, Drizzle ORM, PostgreSQL, MaxKey OIDC, Nextcloud API, LibreOffice

---

## 前置阅读

- [README.md](D:\部门知识共享\knowledge-platform\README.md) — 项目总览、快速开始
- [docs/superpowers/specs/2026-06-19-知识平台总体设计.md](D:\部门知识共享\knowledge-platform\docs\superpowers\specs\2026-06-19-知识平台总体设计.md) — 总体设计
- [design-preview.html](D:\部门知识共享\knowledge-platform\design-preview.html) — UI 设计预览
- `server/db/schema.ts` — 数据库 schema（Drizzle ORM）
- `server/middleware/auth.ts` — 当前认证中间件
- `server/lib/permissions.ts` — 当前权限逻辑
- `server/routers/knowledge.ts` — 知识对象路由（含版本逻辑）
- `src/components/layout/AppLayout.tsx` — 当前布局
- `src/index.css` — 当前样式 token

---

## 文件结构

```
新建:
  server/lib/oidc.ts                    # MaxKey OIDC 客户端
  server/db/migrations/0002_add_external_id.sql
  server/db/migrations/0003_space_members.sql
  server/db/migrations/0004_change_note.sql
  server/db/migrations/0005_knowledge_file_versions.sql
  server/routers/preview.ts             # 文件预览路由
  server/lib/preview.ts                 # LibreOffice 预览转换
  src/components/wiki/RichEditor.tsx    # Tiptap 富文本编辑器
  src/components/common/FilePreview.tsx # 文件预览弹窗
  src/components/common/StatCard.tsx    # 统计卡片组件
  src/lib/utils.ts                      # cn() 工具函数

修改:
  server/db/schema.ts                   # 新增表、枚举、字段
  server/middleware/auth.ts             # OIDC 替换自建 session
  server/lib/permissions.ts             # 空间管理员逻辑
  server/routers/auth.ts                # OIDC 路由
  server/routers/knowledge.ts           # 版本回滚、变更说明
  server/routers/nextcloud.ts           # 增强存储能力
  server/lib/nextcloud.ts               # 增强存储能力
  server/lib/notify.ts                  # 新通知类型
  server/lib/search.ts                  # 权限过滤增强
  src/providers/AuthProvider.tsx        # OIDC 登录流程
  src/components/layout/AppLayout.tsx   # 新侧边栏+顶栏
  src/components/NotificationBell.tsx   # 铃铛通知 UI
  src/pages/Dashboard.tsx               # 新工作台
  src/pages/DocumentDetail.tsx          # 文档详情
  src/pages/DepartmentManagement.tsx    # 部门管理（拆分）
  src/pages/WikiCreate.tsx              # 富文本编辑器
  src/pages/WikiEdit.tsx                # 富文本编辑器
  src/pages/SpaceManagement.tsx         # 空间管理（拆分）
  src/index.css                         # 新设计 token
  src/main.tsx                          # 字体加载
  .env.example                          # 新增 MaxKey 变量
```

---

## Task 1: `cn()` 工具函数

**Files:**
- Create: `src/lib/utils.ts`

- [ ] **Step 1: 创建 `cn()` 工具函数**

```typescript
// src/lib/utils.ts
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

- [ ] **Step 2: 确认依赖已安装**

```bash
npm ls clsx tailwind-merge
```

如果未安装：
```bash
npm install clsx tailwind-merge
```

- [ ] **Step 3: 提交**

```bash
git add src/lib/utils.ts
git commit -m "feat: add cn() utility for Tailwind class merging"
```

---

## Task 2: 设计 Token 与字体加载

**Files:**
- Modify: `src/index.css`
- Modify: `src/main.tsx`

- [ ] **Step 1: 更新 `index.css` 颜色和字体 token**

```css
/* src/index.css */
@import "tailwindcss";
@import url('https://fonts.googleapis.com/css2?family=Noto+Serif+SC:wght@400;600;700&family=Noto+Sans+SC:wght@400;500;600&display=swap');

@theme {
  --color-sidebar: #1e293b;
  --color-sidebar-hover: #334155;
  --color-page-bg: #faf8f5;
  --color-card-bg: #ffffff;
  --color-card-hover: #fdfaf6;
  --color-border: #e7e3de;
  --color-text: #1e293b;
  --color-text-muted: #78716c;
  --color-accent: #d97706;
  --color-accent-light: #fef3c7;
  --color-accent-hover: #b45309;
  --color-success: #15803d;
  --color-success-light: #dcfce7;
  --color-warning: #a16207;
  --color-warning-light: #fef9c3;
  --color-danger: #b91c1c;
  --color-danger-light: #fee2e2;
  --color-info: #1d4ed8;
  --color-info-light: #dbeafe;
  --font-serif: 'Noto Serif SC', serif;
  --font-sans: 'Noto Sans SC', system-ui, sans-serif;
}

body {
  margin: 0;
  font-family: var(--font-sans);
  background: var(--color-page-bg);
  color: var(--color-text);
  line-height: 1.6;
}
```

- [ ] **Step 2: 更新 `main.tsx` 确保字体加载**

```tsx
// src/main.tsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import { BrowserRouter } from 'react-router-dom';
import App from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </React.StrictMode>
);
```

- [ ] **Step 3: 提交**

```bash
git add src/index.css src/main.tsx
git commit -m "style: new design tokens - amber accent, Noto Serif/Sans fonts"
```

---

## Task 3: 导航布局重设计

**Files:**
- Modify: `src/components/layout/AppLayout.tsx`

- [ ] **Step 1: 重写 AppLayout 组件**

```tsx
// src/components/layout/AppLayout.tsx
import { Link, useLocation, Outlet } from "react-router-dom";
import {
  FileText, BookOpen, HardDrive, ClipboardCheck,
  LayoutDashboard, Building2, Users, Layers, Bell
} from "lucide-react";
import { useAuthContext } from "../../providers/AuthProvider";
import { cn } from "../../lib/utils";

const knowledgeLinks = [
  { to: "/", label: "工作台", icon: LayoutDashboard },
  { to: "/documents/new", label: "文档", icon: FileText },
  { to: "/wiki/new", label: "Wiki", icon: BookOpen },
  { to: "/drive", label: "网盘", icon: HardDrive },
  { to: "/review", label: "审核", icon: ClipboardCheck },
];

const adminLinks = [
  { to: "/admin/users", label: "用户管理", icon: Users },
  { to: "/admin/departments", label: "部门管理", icon: Building2 },
  { to: "/admin/spaces", label: "空间管理", icon: Layers },
];

export default function AppLayout() {
  const { user, logout, notificationCount } = useAuthContext();
  const { pathname } = useLocation();

  const isActive = (to: string) =>
    pathname === to || (to !== "/" && pathname.startsWith(to));

  return (
    <div className="flex min-h-screen bg-page-bg">
      {/* Sidebar */}
      <aside className="w-55 bg-sidebar text-slate-300 flex flex-col shrink-0">
        <div className="px-5 py-4 border-b border-white/10">
          <h1 className="font-serif text-lg font-bold text-slate-100 tracking-wide">
            知识平台
          </h1>
          <span className="text-xs text-slate-400 mt-0.5 block">
            企业知识共享中心
          </span>
        </div>

        <nav className="flex-1 px-2.5 py-3 space-y-0.5 overflow-y-auto">
          <div className="text-[11px] text-slate-500 uppercase tracking-wider px-2.5 pt-3 pb-1.5">
            知识管理
          </div>
          {knowledgeLinks.map(({ to, label, icon: Icon }) => (
            <Link
              key={to}
              to={to}
              className={cn(
                "flex items-center gap-2.5 px-2.5 py-2 rounded-md text-sm transition-colors",
                isActive(to)
                  ? "bg-accent text-white font-medium"
                  : "text-slate-300 hover:bg-sidebar-hover hover:text-slate-100"
              )}
            >
              <Icon className="w-[18px] h-[18px] opacity-70" />
              {label}
            </Link>
          ))}

          {user?.role === "admin" && (
            <>
              <div className="text-[11px] text-slate-500 uppercase tracking-wider px-2.5 pt-4 pb-1.5">
                系统管理
              </div>
              {adminLinks.map(({ to, label, icon: Icon }) => (
                <Link
                  key={to}
                  to={to}
                  className={cn(
                    "flex items-center gap-2.5 px-2.5 py-2 rounded-md text-sm transition-colors",
                    isActive(to)
                      ? "bg-accent text-white font-medium"
                      : "text-slate-300 hover:bg-sidebar-hover hover:text-slate-100"
                  )}
                >
                  <Icon className="w-[18px] h-[18px] opacity-70" />
                  {label}
                </Link>
              ))}
            </>
          )}
        </nav>

        <div className="p-3 border-t border-white/10 flex items-center gap-2.5 text-sm">
          <div className="w-7 h-7 rounded-full bg-accent text-white flex items-center justify-center text-xs font-semibold">
            {user?.name?.[0] ?? "U"}
          </div>
          <span>{user?.name}</span>
        </div>
      </aside>

      {/* Main */}
      <div className="flex-1 flex flex-col min-w-0">
        <header className="h-[52px] bg-card-bg border-b border-border flex items-center justify-end gap-4 px-7 shrink-0">
          <Link to="/notifications" className="relative text-text-muted hover:text-text">
            <Bell className="w-5 h-5" />
            {(notificationCount ?? 0) > 0 && (
              <span className="absolute -top-0.5 -right-0.5 w-2 h-2 bg-accent rounded-full border-2 border-white" />
            )}
          </Link>
          <span className="text-sm text-text">{user?.name}</span>
          <button
            onClick={() => logout()}
            className="text-sm text-text-muted hover:text-text"
          >
            退出
          </button>
        </header>
        <main className="flex-1 overflow-y-auto">
          <Outlet />
        </main>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 更新 AuthProvider 以提供 notificationCount**

```tsx
// src/providers/AuthProvider.tsx — 在 Context value 中加入
interface AuthContextType {
  user: User | null;
  logout: () => void;
  notificationCount: number;
  // ... 其余不变
}
```

- [ ] **Step 3: 确认 Nav 路由在 App.tsx 中注册**

```bash
# 检查 App.tsx 中是否有 /admin/departments 和 /admin/spaces 路由
rg "departments|spaces" src/App.tsx
```

如没有，需要新增路由。

- [ ] **Step 4: 提交**

```bash
git add src/components/layout/AppLayout.tsx src/providers/AuthProvider.tsx
git commit -m "feat: redesigned sidebar with dark theme, section grouping, bell notification"
```

---

## Task 4: 统计卡片组件

**Files:**
- Create: `src/components/common/StatCard.tsx`

- [ ] **Step 1: 创建 StatCard 组件**

```tsx
// src/components/common/StatCard.tsx
import { cn } from "../../lib/utils";

interface StatCardProps {
  label: string;
  value: string | number;
  sub?: string;
  colorClass?: string;
}

export default function StatCard({ label, value, sub, colorClass }: StatCardProps) {
  return (
    <div className="bg-card-bg border border-border rounded-lg px-5 py-4.5">
      <div className="text-[13px] text-text-muted mb-1">{label}</div>
      <div className={cn("font-serif text-[26px] font-bold", colorClass)}>
        {value}
      </div>
      {sub && <div className="text-xs text-text-muted mt-0.5">{sub}</div>}
    </div>
  );
}
```

- [ ] **Step 2: 提交**

```bash
git add src/components/common/StatCard.tsx
git commit -m "feat: add StatCard component for dashboard"
```

---

## Task 5: 工作台重设计

**Files:**
- Modify: `src/pages/Dashboard.tsx`

- [ ] **Step 1: 重写 Dashboard 页面**

```tsx
// src/pages/Dashboard.tsx
import { useState } from "react";
import { Link } from "react-router-dom";
import { useQuery } from "@tanstack/react-query";
import { useAuthContext } from "../providers/AuthProvider";
import StatCard from "../components/common/StatCard";
import { cn } from "../lib/utils";

async function api(path: string, options?: RequestInit) {
  const res = await fetch(path, { credentials: "include", headers: { "Content-Type": "application/json" }, ...options });
  if (!res.ok) throw new Error(`Request failed: ${res.status}`);
  return res.json();
}

const typeConfig = {
  document: { label: "文", cls: "bg-info-light text-info" },
  wiki: { label: "Wi", cls: "bg-success-light text-success" },
  drive_file: { label: "文", cls: "bg-warning-light text-warning" },
} as const;

const statusConfig = {
  published: { label: "已发布", cls: "bg-success-light text-success" },
  draft: { label: "草稿", cls: "bg-slate-100 text-slate-500" },
  pending_review: { label: "审核中", cls: "bg-warning-light text-warning" },
  archived: { label: "已归档", cls: "bg-danger-light text-danger" },
} as const;

export default function Dashboard() {
  const { user } = useAuthContext();
  const [search, setSearch] = useState("");

  const { data: recent } = useQuery({
    queryKey: ["knowledge", "recent"],
    queryFn: () => api("/api/knowledge/search"),
  });

  const { data: spaces } = useQuery({
    queryKey: ["spaces"],
    queryFn: () => api("/api/spaces"),
  });

  const objects = recent?.objects ?? [];
  const spaceList = spaces?.spaces ?? [];

  return (
    <div className="px-8 py-7 max-w-7xl">
      {/* Stats */}
      <div className="grid grid-cols-4 gap-4 mb-7">
        <StatCard label="知识总量" value={objects.length} sub="当前可见" colorClass="text-accent" />
        <StatCard label="空间数量" value={spaceList.length} sub={`${spaceList.filter((s: any) => s.autoPublish).length}个自动发布`} colorClass="text-info" />
        <StatCard label="待审核" value="5" sub="3篇文档 · 2篇Wiki" colorClass="text-warning" />
        <StatCard label="今日更新" value="8" sub="文档3 · Wiki2 · 文件3" colorClass="text-success" />
      </div>

      {/* Search */}
      <input
        type="text"
        value={search}
        onChange={(e) => setSearch(e.target.value)}
        placeholder="搜索文档、Wiki、文件…"
        className="w-full max-w-[480px] border border-border rounded-lg px-4 py-2.5 text-sm bg-card-bg mb-7 focus:border-accent focus:ring-3 focus:ring-accent-light outline-none"
      />

      {/* Spaces */}
      {spaceList.length > 0 && (
        <div className="mb-8">
          <div className="flex items-center justify-between mb-3.5">
            <h2 className="font-serif text-lg font-semibold">我的空间</h2>
          </div>
          <div className="flex gap-2 flex-wrap">
            {spaceList.map((s: any) => (
              <Link
                key={s.id}
                to={`/space/${s.id}`}
                className="px-4 py-1.5 bg-card-bg border border-border rounded-md text-sm hover:border-accent hover:bg-accent-light transition-colors"
              >
                {s.name}
              </Link>
            ))}
          </div>
        </div>
      )}

      {/* Recent */}
      <div className="flex items-center justify-between mb-3.5">
        <h2 className="font-serif text-lg font-semibold">最近更新</h2>
      </div>

      {objects.length === 0 ? (
        <div className="text-center py-20">
          <p className="text-text-muted">知识空间为空</p>
          <div className="flex gap-3 justify-center mt-4">
            <Link to="/documents/new" className="btn btn-primary">创建文档</Link>
          </div>
        </div>
      ) : (
        <div className="space-y-1.5">
          {objects.map((obj: any) => {
            const tc = typeConfig[obj.type as keyof typeof typeConfig] ?? { label: "?", cls: "bg-slate-100 text-slate-500" };
            const sc = statusConfig[obj.status as keyof typeof statusConfig] ?? { label: obj.status, cls: "" };
            const linkPath = obj.type === "drive_file" ? undefined : `/knowledge/${obj.id}`;
            const content = (
              <div className="flex items-center bg-card-bg border border-border rounded-lg px-4 py-3 hover:border-accent transition-colors cursor-pointer">
                <span className={cn("w-9 h-9 rounded-md flex items-center justify-center text-[13px] font-semibold mr-3.5 shrink-0", tc.cls)}>
                  {tc.label}
                </span>
                <div className="flex-1 min-w-0">
                  <div className="text-sm font-semibold">{obj.title}</div>
                  <div className="text-xs text-text-muted">
                    {obj.spaceName ?? "—"} · {obj.ownerName ?? "—"} · {new Date(obj.updatedAt).toLocaleDateString("zh-CN")}
                  </div>
                </div>
                <span className={cn("text-xs px-2.5 py-1 rounded-full font-medium ml-3 shrink-0", sc.cls)}>
                  {sc.label}
                </span>
              </div>
            );
            return linkPath ? <Link key={obj.id} to={linkPath}>{content}</Link> : <div key={obj.id}>{content}</div>;
          })}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 添加全局 button 样式到 `index.css`**

```css
/* 在 index.css 末尾追加 */
.btn {
  display: inline-flex; align-items: center; gap: 6px;
  padding: 7px 16px; border-radius: 6px; font-size: 13px; font-weight: 500;
  border: 1px solid var(--color-border); background: var(--color-card-bg);
  color: var(--color-text); cursor: pointer; transition: all 0.15s;
}
.btn:hover { border-color: var(--color-accent) }
.btn-primary { background: var(--color-accent); color: #fff; border-color: var(--color-accent) }
.btn-primary:hover { background: var(--color-accent-hover) }
```

- [ ] **Step 3: 提交**

```bash
git add src/pages/Dashboard.tsx src/index.css
git commit -m "feat: redesigned dashboard with stats, search, space tags, row-based knowledge list"
```

---

## Task 6: 文档列表页面

**Files:**
- Modify: `src/pages/DocumentCreate.tsx`（文档列表页入口）
- 或者创建 `src/pages/DocumentList.tsx` 并注册路由

- [ ] **Step 1: 确定现有"文档"入口路由**
  
```bash
rg "documents/new\|DocumentCreate" src/App.tsx
```

如果 `DocumentCreate.tsx` 是创建页，则需要新建 `DocumentList.tsx` 作为列表页。

- [ ] **Step 2: 实现文档列表页（如需要新建）**

```tsx
// src/pages/DocumentList.tsx
import { useQuery } from "@tanstack/react-query";
import { Link } from "react-router-dom";
import { cn } from "../lib/utils";

async function api(path: string) {
  const res = await fetch(path, { credentials: "include" });
  if (!res.ok) throw new Error("Failed");
  return res.json();
}

const statusConfig: Record<string, { label: string; cls: string }> = {
  published: { label: "已发布", cls: "bg-success-light text-success" },
  draft: { label: "草稿", cls: "bg-slate-100 text-slate-500" },
  pending_review: { label: "审核中", cls: "bg-warning-light text-warning" },
};

export default function DocumentList() {
  const { data, isLoading } = useQuery({
    queryKey: ["knowledge", "search", "document"],
    queryFn: () => api("/api/knowledge/search?type=document"),
  });

  const objects = data?.objects ?? [];

  return (
    <div className="px-8 py-7 max-w-7xl">
      <div className="flex items-center justify-between mb-5">
        <h2 className="font-serif text-xl font-bold">文档</h2>
        <Link to="/documents/new" className="btn btn-primary">+ 新建文档</Link>
      </div>

      <input
        type="text"
        placeholder="搜索文档…"
        className="w-full max-w-xs border border-border rounded-lg px-4 py-2 text-sm bg-card-bg mb-5 outline-none focus:border-accent"
      />

      {isLoading ? (
        <p className="text-sm text-text-muted">加载中…</p>
      ) : (
        <table className="w-full text-sm">
          <thead>
            <tr>
              <th className="text-left px-3.5 py-2.5 text-xs text-text-muted uppercase tracking-wider font-semibold border-b border-border">标题</th>
              <th className="text-left px-3.5 py-2.5 text-xs text-text-muted uppercase tracking-wider font-semibold border-b border-border">作者</th>
              <th className="text-left px-3.5 py-2.5 text-xs text-text-muted uppercase tracking-wider font-semibold border-b border-border">状态</th>
              <th className="text-left px-3.5 py-2.5 text-xs text-text-muted uppercase tracking-wider font-semibold border-b border-border">更新时间</th>
              <th className="text-left px-3.5 py-2.5 text-xs text-text-muted uppercase tracking-wider font-semibold border-b border-border">操作</th>
            </tr>
          </thead>
          <tbody>
            {objects.map((obj: any) => {
              const sc = statusConfig[obj.status] ?? { label: obj.status, cls: "" };
              return (
                <tr key={obj.id} className="hover:bg-card-hover transition-colors">
                  <td className="px-3.5 py-2.5 font-semibold border-b border-border">
                    <Link to={`/knowledge/${obj.id}`} className="hover:text-accent">{obj.title}</Link>
                  </td>
                  <td className="px-3.5 py-2.5 text-text-muted border-b border-border">{obj.ownerName ?? "—"}</td>
                  <td className="px-3.5 py-2.5 border-b border-border">
                    <span className={cn("text-xs px-2.5 py-1 rounded-full font-medium", sc.cls)}>{sc.label}</span>
                  </td>
                  <td className="px-3.5 py-2.5 text-text-muted border-b border-border">
                    {new Date(obj.updatedAt).toLocaleDateString("zh-CN")}
                  </td>
                  <td className="px-3.5 py-2.5 border-b border-border">
                    <Link to={`/knowledge/${obj.id}`} className="btn btn-sm text-xs">查看</Link>
                  </td>
                </tr>
              );
            })}
          </tbody>
        </table>
      )}
    </div>
  );
}
```

- [ ] **Step 3: 注册路由**

```tsx
// src/App.tsx — 添加 DocumentList 路由
<Route path="/documents" element={<DocumentList />} />
<Route path="/documents/new" element={<DocumentCreate />} />
```

- [ ] **Step 4: 更新侧边栏链接**

```tsx
// AppLayout.tsx — 将 "/documents/new" 改为 "/documents"
{ to: "/documents", label: "文档", icon: FileText },
```

- [ ] **Step 5: 提交**

```bash
git add src/pages/DocumentList.tsx src/App.tsx src/components/layout/AppLayout.tsx
git commit -m "feat: add document list page with table view"
```

---

## Task 7: 用户管理页面

**Files:**
- Modify: `src/pages/UserManagement.tsx`

- [ ] **Step 1: 检查现有文件**

```bash
ls src/pages/UserManagement.tsx
```

- [ ] **Step 2: 重写 UserManagement 为新设计风格**

```tsx
// src/pages/UserManagement.tsx
import { useQuery } from "@tanstack/react-query";
import { cn } from "../lib/utils";

async function api(path: string, options?: RequestInit) {
  const res = await fetch(path, { credentials: "include", headers: { "Content-Type": "application/json" }, ...options });
  if (!res.ok) throw new Error(`Failed: ${res.status}`);
  return res.json();
}

const roleConfig: Record<string, string> = {
  admin: "bg-accent-light text-accent",
  editor: "bg-warning-light text-warning",
  reviewer: "bg-warning-light text-warning",
  viewer: "bg-slate-100 text-slate-500",
};

export default function UserManagement() {
  const { data, isLoading } = useQuery({
    queryKey: ["users"],
    queryFn: () => api("/api/users"),
  });

  const users = data?.users ?? [];

  return (
    <div className="px-8 py-7 max-w-7xl">
      <div className="flex items-center justify-between mb-5">
        <h2 className="font-serif text-xl font-bold">用户管理</h2>
        <button className="btn btn-primary">+ 添加用户</button>
      </div>

      <input
        type="text"
        placeholder="搜索用户名或邮箱…"
        className="w-full max-w-xs border border-border rounded-lg px-4 py-2 text-sm bg-card-bg mb-5 outline-none focus:border-accent"
      />

      {isLoading ? (
        <p className="text-sm text-text-muted">加载中…</p>
      ) : (
        <table className="w-full text-sm">
          <thead>
            <tr>
              <th className="text-left px-3.5 py-2.5 text-xs text-text-muted uppercase tracking-wider font-semibold border-b border-border">用户名</th>
              <th className="text-left px-3.5 py-2.5 text-xs text-text-muted uppercase tracking-wider font-semibold border-b border-border">姓名</th>
              <th className="text-left px-3.5 py-2.5 text-xs text-text-muted uppercase tracking-wider font-semibold border-b border-border">邮箱</th>
              <th className="text-left px-3.5 py-2.5 text-xs text-text-muted uppercase tracking-wider font-semibold border-b border-border">部门</th>
              <th className="text-left px-3.5 py-2.5 text-xs text-text-muted uppercase tracking-wider font-semibold border-b border-border">角色</th>
              <th className="text-left px-3.5 py-2.5 text-xs text-text-muted uppercase tracking-wider font-semibold border-b border-border">状态</th>
              <th className="text-left px-3.5 py-2.5 text-xs text-text-muted uppercase tracking-wider font-semibold border-b border-border">操作</th>
            </tr>
          </thead>
          <tbody>
            {users.map((u: any) => (
              <tr key={u.id} className="hover:bg-card-hover transition-colors">
                <td className="px-3.5 py-2.5 font-semibold border-b border-border">{u.name}</td>
                <td className="px-3.5 py-2.5 border-b border-border">{u.name}</td>
                <td className="px-3.5 py-2.5 text-text-muted border-b border-border">{u.email}</td>
                <td className="px-3.5 py-2.5 border-b border-border">{u.departmentName ?? "—"}</td>
                <td className="px-3.5 py-2.5 border-b border-border">
                  <span className={cn("text-xs px-2.5 py-1 rounded-full font-medium", roleConfig[u.role] ?? "bg-slate-100 text-slate-500")}>
                    {u.role === "admin" ? "管理员" : u.role === "reviewer" ? "审核员" : u.role === "editor" ? "编辑者" : "普通用户"}
                  </span>
                </td>
                <td className="px-3.5 py-2.5 border-b border-border">
                  {u.disabled ? (
                    <span className="text-danger text-[13px]">● 已禁用</span>
                  ) : (
                    <span className="text-success text-[13px]">● 正常</span>
                  )}
                </td>
                <td className="px-3.5 py-2.5 border-b border-border">
                  <button className="btn btn-sm text-xs">编辑</button>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      )}
    </div>
  );
}
```

- [ ] **Step 3: 提交**

```bash
git add src/pages/UserManagement.tsx
git commit -m "style: redesign user management page with table view and new design tokens"
```

---

## Task 8: 部门管理页面（从 DepartmentManagement 中拆分）

**Files:**
- Create: `src/pages/DepartmentManagement.tsx`（重写）
- Create: `src/pages/SpaceManagement.tsx`（拆分空间管理）

- [ ] **Step 1: 重写 DepartmentManagement 为纯部门管理**

```tsx
// src/pages/DepartmentManagement.tsx
import { useState } from "react";
import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";

async function api(path: string, options?: RequestInit) {
  const res = await fetch(path, { credentials: "include", headers: { "Content-Type": "application/json" }, ...options });
  if (!res.ok) throw new Error(`Failed: ${res.status}`);
  return res.json();
}

export default function DepartmentManagement() {
  const queryClient = useQueryClient();
  const [newName, setNewName] = useState("");

  const { data, isLoading } = useQuery({
    queryKey: ["departments"],
    queryFn: () => api("/api/departments"),
  });

  const createMut = useMutation({
    mutationFn: (name: string) => api("/api/departments", { method: "POST", body: JSON.stringify({ name }) }),
    onSuccess: () => { queryClient.invalidateQueries({ queryKey: ["departments"] }); setNewName(""); },
  });

  const deleteMut = useMutation({
    mutationFn: (id: string) => api(`/api/departments/${id}`, { method: "DELETE" }),
    onSuccess: () => queryClient.invalidateQueries({ queryKey: ["departments"] }),
  });

  const deps = data?.departments ?? [];

  return (
    <div className="px-8 py-7 max-w-4xl">
      <div className="flex items-center justify-between mb-5">
        <h2 className="font-serif text-xl font-bold">部门管理</h2>
        <div className="flex gap-2">
          <input
            value={newName}
            onChange={(e) => setNewName(e.target.value)}
            placeholder="新部门名称"
            className="px-3 py-2 border border-border rounded-lg text-sm bg-card-bg outline-none focus:border-accent"
          />
          <button
            onClick={() => newName && createMut.mutate(newName)}
            className="btn btn-primary"
          >
            创建
          </button>
        </div>
      </div>

      {isLoading ? (
        <p className="text-sm text-text-muted">加载中…</p>
      ) : (
        <table className="w-full text-sm">
          <thead>
            <tr>
              <th className="text-left px-3.5 py-2.5 text-xs text-text-muted uppercase tracking-wider font-semibold border-b border-border">部门名称</th>
              <th className="text-left px-3.5 py-2.5 text-xs text-text-muted uppercase tracking-wider font-semibold border-b border-border">创建时间</th>
              <th className="text-left px-3.5 py-2.5 text-xs text-text-muted uppercase tracking-wider font-semibold border-b border-border">操作</th>
            </tr>
          </thead>
          <tbody>
            {deps.map((d: any) => (
              <tr key={d.id} className="hover:bg-card-hover transition-colors">
                <td className="px-3.5 py-2.5 font-semibold border-b border-border">{d.name}</td>
                <td className="px-3.5 py-2.5 text-text-muted border-b border-border">
                  {new Date(d.createdAt).toLocaleDateString("zh-CN")}
                </td>
                <td className="px-3.5 py-2.5 border-b border-border">
                  <button
                    onClick={() => deleteMut.mutate(d.id)}
                    className="text-xs text-danger hover:underline"
                  >
                    删除
                  </button>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 创建独立的空间管理页面 `SpaceManagement.tsx`**

```tsx
// src/pages/SpaceManagement.tsx
import { useState } from "react";
import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";

async function api(path: string, options?: RequestInit) {
  const res = await fetch(path, { credentials: "include", headers: { "Content-Type": "application/json" }, ...options });
  if (!res.ok) throw new Error(`Failed: ${res.status}`);
  return res.json();
}

export default function SpaceManagement() {
  const queryClient = useQueryClient();
  const [spaceName, setSpaceName] = useState("");
  const [spaceDept, setSpaceDept] = useState("");

  const { data: depsData } = useQuery({ queryKey: ["departments"], queryFn: () => api("/api/departments") });
  const { data: spacesData } = useQuery({ queryKey: ["spaces"], queryFn: () => api("/api/spaces") });

  const createSpace = useMutation({
    mutationFn: (body: { name: string; departmentId: string }) => api("/api/spaces", { method: "POST", body: JSON.stringify(body) }),
    onSuccess: () => { queryClient.invalidateQueries({ queryKey: ["spaces"] }); setSpaceName(""); },
  });

  const deps = depsData?.departments ?? [];
  const spaces = spacesData?.spaces ?? [];

  return (
    <div className="px-8 py-7 max-w-7xl">
      <div className="flex items-center justify-between mb-5">
        <h2 className="font-serif text-xl font-bold">空间管理</h2>
        <div className="flex gap-2">
          <input
            value={spaceName}
            onChange={(e) => setSpaceName(e.target.value)}
            placeholder="空间名称"
            className="px-3 py-2 border border-border rounded-lg text-sm bg-card-bg outline-none focus:border-accent"
          />
          <select
            value={spaceDept}
            onChange={(e) => setSpaceDept(e.target.value)}
            className="px-3 py-2 border border-border rounded-lg text-sm bg-card-bg"
          >
            <option value="">选择部门</option>
            {deps.map((d: any) => <option key={d.id} value={d.id}>{d.name}</option>)}
          </select>
          <button
            onClick={() => spaceName && spaceDept && createSpace.mutate({ name: spaceName, departmentId: spaceDept })}
            className="btn btn-primary"
          >
            创建
          </button>
        </div>
      </div>

      <div className="grid grid-cols-3 gap-4">
        {spaces.map((s: any) => {
          const dept = deps.find((d: any) => d.id === s.departmentId);
          return (
            <div key={s.id} className="bg-card-bg border border-border rounded-lg p-5">
              <div className="flex justify-between items-start mb-1">
                <h3 className="font-semibold text-[15px]">{s.name}</h3>
                <span className="text-[11px] bg-success-light text-success px-2 py-0.5 rounded-md">活跃</span>
              </div>
              <p className="text-[13px] text-text-muted mb-3.5">{dept?.name ?? "—"} · {s.autoPublish ? "自动发布" : "需审核"}</p>
              <div className="flex gap-2">
                <button className="btn btn-sm text-xs">编辑</button>
                <button className="btn btn-sm text-xs">设置</button>
              </div>
            </div>
          );
        })}
        <div className="bg-card-bg border border-dashed border-border rounded-lg flex items-center justify-center min-h-[100px] cursor-pointer">
          <span className="text-text-muted text-sm">+ 新建空间</span>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: 更新路由**

```tsx
// src/App.tsx — 确保有以下路由
<Route path="/admin/departments" element={<DepartmentManagement />} />
<Route path="/admin/spaces" element={<SpaceManagement />} />
```

- [ ] **Step 4: 提交**

```bash
git add src/pages/DepartmentManagement.tsx src/pages/SpaceManagement.tsx src/App.tsx
git commit -m "refactor: split department and space management into separate pages"
```

---

## Task 9: 通知中心页面

**Files:**
- Create: `src/pages/NotificationCenter.tsx`
- Modify: `src/components/NotificationBell.tsx`

- [ ] **Step 1: 创建通知中心页面**

```tsx
// src/pages/NotificationCenter.tsx
import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";
import { cn } from "../lib/utils";

async function api(path: string, options?: RequestInit) {
  const res = await fetch(path, { credentials: "include", headers: { "Content-Type": "application/json" }, ...options });
  if (!res.ok) throw new Error(`Failed: ${res.status}`);
  return res.json();
}

export default function NotificationCenter() {
  const queryClient = useQueryClient();

  const { data } = useQuery({
    queryKey: ["notifications"],
    queryFn: () => api("/api/notifications"),
  });

  const markReadMut = useMutation({
    mutationFn: (id: string) => api(`/api/notifications/${id}/read`, { method: "POST" }),
    onSuccess: () => queryClient.invalidateQueries({ queryKey: ["notifications"] }),
  });

  const markAllReadMut = useMutation({
    mutationFn: () => api("/api/notifications/read-all", { method: "POST" }),
    onSuccess: () => queryClient.invalidateQueries({ queryKey: ["notifications"] }),
  });

  const notifications = data?.notifications ?? [];

  const typeLabels: Record<string, string> = {
    review_requested: "审核任务",
    review_approved: "审核通过",
    review_rejected: "审核驳回",
    new_version: "文档更新",
    mentioned: "提及",
  };

  return (
    <div className="px-8 py-7 max-w-3xl">
      <div className="flex items-center justify-between mb-5">
        <h2 className="font-serif text-xl font-bold">通知中心</h2>
        <button onClick={() => markAllReadMut.mutate()} className="btn btn-sm text-xs">全部标为已读</button>
      </div>

      <div className="space-y-0.5">
        {notifications.map((n: any) => (
          <div
            key={n.id}
            onClick={() => !n.read && markReadMut.mutate(n.id)}
            className={cn(
              "flex items-start gap-3 px-3.5 py-3 rounded-md cursor-pointer transition-colors",
              n.read ? "" : "bg-accent-light"
            )}
          >
            {!n.read && <span className="w-2 h-2 bg-accent rounded-full mt-[7px] shrink-0" />}
            <div className="flex-1">
              <div className="text-sm font-medium">{typeLabels[n.type] ?? n.type}</div>
              <div className="text-[13px] text-text-muted mt-0.5">{n.message}</div>
              <div className="text-xs text-slate-400 mt-1">
                {new Date(n.createdAt).toLocaleString("zh-CN")}
              </div>
            </div>
          </div>
        ))}
        {notifications.length === 0 && (
          <p className="text-center py-16 text-text-muted">暂无通知</p>
        )}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 更新 NotificationBell 组件**

```tsx
// src/components/NotificationBell.tsx
import { Bell } from "lucide-react";
import { Link } from "react-router-dom";
import { useAuthContext } from "../providers/AuthProvider";

export default function NotificationBell() {
  const { notificationCount } = useAuthContext();

  return (
    <Link to="/notifications" className="relative text-text-muted hover:text-text">
      <Bell className="w-5 h-5" />
      {(notificationCount ?? 0) > 0 && (
        <span className="absolute -top-1 -right-1 w-[18px] h-[18px] bg-accent text-white text-[10px] font-semibold rounded-full flex items-center justify-center">
          {notificationCount}
        </span>
      )}
    </Link>
  );
}
```

- [ ] **Step 3: 注册路由**

```tsx
// src/App.tsx
<Route path="/notifications" element={<NotificationCenter />} />
```

- [ ] **Step 4: 提交**

```bash
git add src/pages/NotificationCenter.tsx src/components/NotificationBell.tsx src/App.tsx
git commit -m "feat: add notification center page and bell component"
```

---

## Task 10: 文件预览弹窗

**Files:**
- Create: `src/components/common/FilePreview.tsx`

- [ ] **Step 1: 创建文件预览弹窗组件**

```tsx
// src/components/common/FilePreview.tsx
import { useState } from "react";
import { X, Download } from "lucide-react";

interface FilePreviewProps {
  open: boolean;
  onClose: () => void;
  filePath: string;
  fileName: string;
  fileType: string;
  downloadUrl: string;
}

export default function FilePreview({ open, onClose, filePath, fileName, fileType, downloadUrl }: FilePreviewProps) {
  const [loading, setLoading] = useState(true);

  if (!open) return null;

  const isOffice = /\.(docx?|xlsx?|pptx?)$/i.test(fileName);
  const previewUrl = isOffice
    ? `/api/preview/office?path=${encodeURIComponent(filePath)}`
    : `/api/preview/raw?path=${encodeURIComponent(filePath)}`;

  return (
    <div className="fixed inset-0 z-50 bg-black/50 flex items-center justify-center" onClick={onClose}>
      <div
        className="bg-card-bg rounded-lg w-[90vw] h-[85vh] flex flex-col shadow-xl"
        onClick={(e) => e.stopPropagation()}
      >
        {/* Header */}
        <div className="flex items-center justify-between px-5 py-3 border-b border-border shrink-0">
          <h3 className="font-semibold text-sm truncate">{fileName}</h3>
          <div className="flex gap-2">
            <a href={downloadUrl} download className="btn btn-sm text-xs flex items-center gap-1">
              <Download className="w-3.5 h-3.5" /> 下载
            </a>
            <button onClick={onClose} className="text-text-muted hover:text-text">
              <X className="w-5 h-5" />
            </button>
          </div>
        </div>

        {/* Content */}
        <div className="flex-1 overflow-auto">
          {loading && (
            <div className="flex items-center justify-center h-full text-text-muted text-sm">
              {isOffice ? "正在转换格式…" : "加载中…"}
            </div>
          )}
          {isOffice ? (
            <iframe
              src={previewUrl}
              onLoad={() => setLoading(false)}
              className="w-full h-full border-0"
            />
          ) : (
            <iframe
              src={previewUrl}
              onLoad={() => setLoading(false)}
              className="w-full h-full border-0"
            />
          )}
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 在文档详情页面中使用 FilePreview**

在 `DocumentDetail.tsx` 中引入并使用 `FilePreview`，具体集成在 Task 12 的文档详情重构中完成。

- [ ] **Step 3: 提交**

```bash
git add src/components/common/FilePreview.tsx
git commit -m "feat: add file preview modal component"
```

---

## Task 11: 文档详情页面重构

**Files:**
- Modify: `src/pages/DocumentDetail.tsx`

- [ ] **Step 1: 重构 DocumentDetail 页面**

```tsx
// src/pages/DocumentDetail.tsx
import { useState } from "react";
import { useParams, Link } from "react-router-dom";
import { useQuery } from "@tanstack/react-query";
import { cn } from "../lib/utils";
import FilePreview from "../components/common/FilePreview";

async function api(path: string) {
  const res = await fetch(path, { credentials: "include" });
  if (!res.ok) throw new Error("Failed");
  return res.json();
}

const statusConfig: Record<string, { label: string; cls: string }> = {
  published: { label: "已发布", cls: "bg-success-light text-success" },
  draft: { label: "草稿", cls: "bg-slate-100 text-slate-500" },
  pending_review: { label: "审核中", cls: "bg-warning-light text-warning" },
  archived: { label: "已归档", cls: "bg-danger-light text-danger" },
};

export default function DocumentDetail() {
  const { id } = useParams<{ id: string }>();
  const [previewFile, setPreviewFile] = useState<{ path: string; name: string; type: string; downloadUrl: string } | null>(null);

  const { data, isLoading } = useQuery({
    queryKey: ["knowledge", id],
    queryFn: () => api(`/api/knowledge/${id}`),
    enabled: !!id,
  });

  const obj = data?.object;
  const versions = data?.versions ?? [];

  if (isLoading) return <div className="px-8 py-7 text-text-muted">加载中…</div>;
  if (!obj) return <div className="px-8 py-7 text-danger">文档不存在</div>;

  const sc = statusConfig[obj.status] ?? { label: obj.status, cls: "" };

  return (
    <div className="px-8 py-7 max-w-4xl">
      {/* Breadcrumb */}
      <div className="flex gap-1.5 items-center text-[13px] text-text-muted mb-5">
        <Link to="/" className="text-accent hover:underline">工作台</Link>
        <span>/</span>
        <span>{obj.spaceName ?? "—"}</span>
        <span>/</span>
        <span className="text-text">{obj.title}</span>
      </div>

      {/* Header */}
      <div className="mb-6">
        <h2 className="font-serif text-2xl font-bold mb-2">{obj.title}</h2>
        <div className="flex flex-wrap gap-4 text-[13px] text-text-muted">
          <span>作者：{obj.ownerName ?? "—"}</span>
          <span>原产地：{obj.ownerId ?? "—"}</span>
          <span>版本：v{obj.version}</span>
          <span className={cn("text-xs px-2.5 py-1 rounded-full font-medium", sc.cls)}>{sc.label}</span>
        </div>
        <div className="flex gap-2 mt-3">
          <button className="btn btn-primary btn-sm">预览</button>
          <button className="btn btn-sm">下载</button>
          <button className="btn btn-sm">编辑</button>
          <button className="btn btn-sm ml-auto text-danger border-danger-light">删除</button>
        </div>
      </div>

      {/* Tabs */}
      <div className="flex border-b-2 border-border mb-5">
        {["基本信息", "版本历史", "权限设置", "审核记录"].map((t, i) => (
          <button
            key={t}
            className={cn(
              "px-5 py-2 text-sm border-b-2 border-transparent -mb-0.5 transition-colors",
              i === 0 ? "text-accent border-accent font-semibold" : "text-text-muted hover:text-text"
            )}
          >
            {t}
          </button>
        ))}
      </div>

      {/* Description */}
      <div className="bg-card-bg border border-border rounded-lg p-5 mb-5">
        <h3 className="text-[15px] font-semibold mb-3">文档说明</h3>
        <p className="text-sm leading-relaxed">{obj.description || "暂无说明"}</p>
      </div>

      {/* Attachments */}
      <div className="bg-card-bg border border-border rounded-lg p-5 mb-5">
        <h3 className="text-[15px] font-semibold mb-3">附件文件</h3>
        {obj.filePath ? (
          <div className="flex items-center bg-page-bg border border-border rounded-lg px-4 py-2.5">
            <span className="text-sm font-medium flex-1">{obj.fileName ?? obj.filePath.split("/").pop()}</span>
            <button className="btn btn-sm text-xs" onClick={() => setPreviewFile({
              path: obj.filePath,
              name: obj.fileName ?? "文件",
              type: obj.fileType ?? "application/octet-stream",
              downloadUrl: `/api/drive/download?path=${encodeURIComponent(obj.filePath)}`,
            })}>预览</button>
            <a href={`/api/drive/download?path=${encodeURIComponent(obj.filePath)}`} className="btn btn-sm text-xs ml-1">下载</a>
          </div>
        ) : (
          <p className="text-sm text-text-muted">暂无附件</p>
        )}
      </div>

      {/* Version History */}
      <div className="bg-card-bg border border-border rounded-lg p-5">
        <h3 className="text-[15px] font-semibold mb-3">版本历史</h3>
        <table className="w-full text-sm">
          <thead>
            <tr>
              <th className="text-left px-3 py-2 text-xs text-text-muted uppercase tracking-wider font-semibold border-b border-border">版本</th>
              <th className="text-left px-3 py-2 text-xs text-text-muted uppercase tracking-wider font-semibold border-b border-border">变更说明</th>
              <th className="text-left px-3 py-2 text-xs text-text-muted uppercase tracking-wider font-semibold border-b border-border">更新时间</th>
              <th className="text-left px-3 py-2 text-xs text-text-muted uppercase tracking-wider font-semibold border-b border-border">操作</th>
            </tr>
          </thead>
          <tbody>
            {versions.map((v: any) => {
              const vs = statusConfig[v.status] ?? { label: v.status, cls: "" };
              return (
                <tr key={v.id} className="hover:bg-card-hover transition-colors">
                  <td className="px-3 py-2 font-semibold border-b border-border">
                    v{v.version} <span className={cn("text-[11px] px-1.5 py-0.5 rounded ml-1", vs.cls)}>{vs.label}</span>
                  </td>
                  <td className="px-3 py-2 text-text-muted border-b border-border">{v.changeNote || "—"}</td>
                  <td className="px-3 py-2 text-text-muted border-b border-border">{new Date(v.updatedAt).toLocaleString("zh-CN")}</td>
                  <td className="px-3 py-2 border-b border-border">
                    {v.status === "published" && <button className="btn btn-xs text-xs">下载</button>}
                  </td>
                </tr>
              );
            })}
          </tbody>
        </table>
      </div>

      {previewFile && <FilePreview {...previewFile} onClose={() => setPreviewFile(null)} />}
    </div>
  );
}
```

- [ ] **Step 2: 提交**

```bash
git add src/pages/DocumentDetail.tsx
git commit -m "style: redesign document detail page with tabs, file preview, version history"
```

---

## Task 12: Wiki 编辑器（Tiptap 富文本）

**Files:**
- Create: `src/components/wiki/RichEditor.tsx`
- Modify: `src/pages/WikiCreate.tsx`
- Modify: `src/pages/WikiEdit.tsx`

- [ ] **Step 1: 安装 Tiptap**

```bash
npm install @tiptap/react @tiptap/starter-kit @tiptap/extension-image @tiptap/extension-table @tiptap/extension-table-row @tiptap/extension-table-cell @tiptap/extension-table-header @tiptap/extension-task-list @tiptap/extension-task-item @tiptap/extension-underline @tiptap/extension-placeholder
```

- [ ] **Step 2: 创建 RichEditor 组件**

```tsx
// src/components/wiki/RichEditor.tsx
import { useEditor, EditorContent } from "@tiptap/react";
import StarterKit from "@tiptap/starter-kit";
import Image from "@tiptap/extension-image";
import Table from "@tiptap/extension-table";
import TableRow from "@tiptap/extension-table-row";
import TableCell from "@tiptap/extension-table-cell";
import TableHeader from "@tiptap/extension-table-header";
import TaskList from "@tiptap/extension-task-list";
import TaskItem from "@tiptap/extension-task-item";
import Underline from "@tiptap/extension-underline";
import Placeholder from "@tiptap/extension-placeholder";
import {
  Bold, Italic, Underline as UnderlineIcon, Strikethrough,
  List, ListOrdered, CheckSquare, Image as ImageIcon,
  Table as TableIcon, Quote, Heading1, Heading2, Heading3,
  Undo, Redo, Minus
} from "lucide-react";
import { cn } from "../../lib/utils";

interface RichEditorProps {
  content: string;
  onChange: (html: string) => void;
  placeholder?: string;
}

export default function RichEditor({ content, onChange, placeholder = "开始编写内容…" }: RichEditorProps) {
  const editor = useEditor({
    extensions: [
      StarterKit.configure({ heading: { levels: [1, 2, 3] } }),
      Underline,
      Image.configure({ allowBase64: true }),
      Table.configure({ resizable: true }),
      TableRow, TableCell, TableHeader,
      TaskList, TaskItem.configure({ nested: true }),
      Placeholder.configure({ placeholder }),
    ],
    content,
    onUpdate: ({ editor }) => onChange(editor.getHTML()),
    editorProps: {
      attributes: {
        class: "prose prose-sm max-w-none min-h-[300px] px-4 py-3 outline-none focus:outline-none",
      },
    },
  });

  if (!editor) return null;

  const ToolBtn = ({ onClick, active, children }: { onClick: () => void; active?: boolean; children: React.ReactNode }) => (
    <button
      type="button"
      onClick={onClick}
      className={cn(
        "p-1.5 rounded hover:bg-page-bg transition-colors text-text-muted",
        active && "bg-accent-light text-accent"
      )}
    >
      {children}
    </button>
  );

  const addTable = () => {
    editor.chain().focus().insertTable({ rows: 3, cols: 3, withHeaderRow: true }).run();
  };

  return (
    <div className="border border-border rounded-lg overflow-hidden">
      {/* Toolbar */}
      <div className="flex flex-wrap gap-0.5 px-2 py-1.5 border-b border-border bg-page-bg">
        <ToolBtn onClick={() => editor.chain().focus().toggleBold().run()} active={editor.isActive("bold")}>
          <Bold className="w-4 h-4" />
        </ToolBtn>
        <ToolBtn onClick={() => editor.chain().focus().toggleItalic().run()} active={editor.isActive("italic")}>
          <Italic className="w-4 h-4" />
        </ToolBtn>
        <ToolBtn onClick={() => editor.chain().focus().toggleUnderline().run()} active={editor.isActive("underline")}>
          <UnderlineIcon className="w-4 h-4" />
        </ToolBtn>
        <ToolBtn onClick={() => editor.chain().focus().toggleStrike().run()} active={editor.isActive("strike")}>
          <Strikethrough className="w-4 h-4" />
        </ToolBtn>
        <span className="w-px bg-border mx-1" />
        <ToolBtn onClick={() => editor.chain().focus().toggleHeading({ level: 1 }).run()} active={editor.isActive("heading", { level: 1 })}>
          <Heading1 className="w-4 h-4" />
        </ToolBtn>
        <ToolBtn onClick={() => editor.chain().focus().toggleHeading({ level: 2 }).run()} active={editor.isActive("heading", { level: 2 })}>
          <Heading2 className="w-4 h-4" />
        </ToolBtn>
        <ToolBtn onClick={() => editor.chain().focus().toggleHeading({ level: 3 }).run()} active={editor.isActive("heading", { level: 3 })}>
          <Heading3 className="w-4 h-4" />
        </ToolBtn>
        <span className="w-px bg-border mx-1" />
        <ToolBtn onClick={() => editor.chain().focus().toggleBulletList().run()} active={editor.isActive("bulletList")}>
          <List className="w-4 h-4" />
        </ToolBtn>
        <ToolBtn onClick={() => editor.chain().focus().toggleOrderedList().run()} active={editor.isActive("orderedList")}>
          <ListOrdered className="w-4 h-4" />
        </ToolBtn>
        <ToolBtn onClick={() => editor.chain().focus().toggleTaskList().run()} active={editor.isActive("taskList")}>
          <CheckSquare className="w-4 h-4" />
        </ToolBtn>
        <span className="w-px bg-border mx-1" />
        <ToolBtn onClick={() => editor.chain().focus().toggleBlockquote().run()} active={editor.isActive("blockquote")}>
          <Quote className="w-4 h-4" />
        </ToolBtn>
        <ToolBtn onClick={() => editor.chain().focus().setHorizontalRule().run()}>
          <Minus className="w-4 h-4" />
        </ToolBtn>
        <ToolBtn onClick={addTable}>
          <TableIcon className="w-4 h-4" />
        </ToolBtn>
        <ToolBtn onClick={() => {
          const url = window.prompt("图片地址") || "";
          if (url) editor.chain().focus().setImage({ src: url }).run();
        }}>
          <ImageIcon className="w-4 h-4" />
        </ToolBtn>
        <span className="w-px bg-border mx-1" />
        <ToolBtn onClick={() => editor.chain().focus().undo().run()}>
          <Undo className="w-4 h-4" />
        </ToolBtn>
        <ToolBtn onClick={() => editor.chain().focus().redo().run()}>
          <Redo className="w-4 h-4" />
        </ToolBtn>
      </div>

      {/* Editor area */}
      <EditorContent editor={editor} />
    </div>
  );
}
```

- [ ] **Step 3: 修改 WikiCreate 使用 RichEditor**

```tsx
// src/pages/WikiCreate.tsx
// 将现有的 textarea 替换为:
import RichEditor from "../components/wiki/RichEditor";

// 在表单中:
<RichEditor content={description} onChange={setDescription} placeholder="请输入 Wiki 内容…" />
```

- [ ] **Step 4: 同样修改 WikiEdit.tsx**

- [ ] **Step 5: 提交**

```bash
git add src/components/wiki/RichEditor.tsx src/pages/WikiCreate.tsx src/pages/WikiEdit.tsx
git commit -m "feat: add Tiptap rich text editor for Wiki"
```

---

## Task 13: MaxKey OIDC 认证集成（后端）

**Files:**
- Create: `server/lib/oidc.ts`
- Modify: `server/middleware/auth.ts`
- Modify: `server/routers/auth.ts`
- Modify: `server/db/schema.ts`
- Create: `server/db/migrations/0002_add_external_id.sql`
- Modify: `.env.example`

- [ ] **Step 1: 安装 OIDC 依赖**

```bash
npm install openid-client
npm install -D @types/passport
```

- [ ] **Step 2: 更新 `.env.example`**

```env
# MaxKey OIDC Configuration
MAXKEY_ISSUER=https://maxkey.example.com
MAXKEY_CLIENT_ID=knowledge-platform
MAXKEY_CLIENT_SECRET=your-client-secret
MAXKEY_REDIRECT_URI=http://localhost:3002/api/auth/callback
```

- [ ] **Step 3: 创建 OIDC 客户端 `server/lib/oidc.ts`**

```typescript
// server/lib/oidc.ts
import { Issuer, generators } from "openid-client";

let client: any = null;

export async function getClient() {
  if (client) return client;

  const issuer = await Issuer.discover(process.env.MAXKEY_ISSUER!);
  client = new issuer.Client({
    client_id: process.env.MAXKEY_CLIENT_ID!,
    client_secret: process.env.MAXKEY_CLIENT_SECRET!,
    redirect_uris: [process.env.MAXKEY_REDIRECT_URI!],
    response_types: ["code"],
  });

  return client;
}

export function getLoginUrl() {
  const codeVerifier = generators.codeVerifier();
  const codeChallenge = generators.codeChallenge(codeVerifier);
  return { url: `...`, codeVerifier }; // 简化示意
}
```

实际实现需要完整处理 code_verifier、state、nonce。

- [ ] **Step 4: 添加 `externalId` 字段到 users schema**

```typescript
// server/db/schema.ts — users 表中新增
export const users = pgTable("users", {
  // ... existing fields
  externalId: varchar("external_id", { length: 255 }).unique(),
  passwordHash: text("password_hash"), // 改为 nullable
});
```

- [ ] **Step 5: 创建 migration**

```sql
-- server/db/migrations/0002_add_external_id.sql
ALTER TABLE users ADD COLUMN external_id VARCHAR(255) UNIQUE;
ALTER TABLE users ALTER COLUMN password_hash DROP NOT NULL;
```

- [ ] **Step 6: 更新 auth 中间件**

```typescript
// server/middleware/auth.ts — 保留 session 验证逻辑，新增 OIDC 登录和回调路由
// /api/auth/login    → 重定向到 MaxKey
// /api/auth/callback → 处理 MaxKey 回调，查/建用户，生成 session
// /api/auth/logout   → 销毁 session + MaxKey 登出
// /api/auth/me       → 返回当前用户（保持不变）
```

- [ ] **Step 7: 提交**

```bash
git add server/lib/oidc.ts server/middleware/auth.ts server/routers/auth.ts server/db/schema.ts server/db/migrations/ .env.example
git commit -m "feat: integrate MaxKey OIDC authentication"
```

---

## Task 14: 前端 OIDC 登录适配

**Files:**
- Modify: `src/providers/AuthProvider.tsx`

- [ ] **Step 1: 更新 AuthProvider**

```tsx
// src/providers/AuthProvider.tsx
// 将 `/api/login` 改为检测 401 → window.location.href = '/api/auth/login'
// 移除用户名密码登录表单逻辑
// checkAuth 改为调 `/api/auth/me`
// logout 改为调 `/api/auth/logout`
```

- [ ] **Step 2: 确保 Login 页面对自建登录做兼容处理**

```tsx
// src/pages/Login.tsx
// 如果 MaxKey 未配置（无 MAXKEY_ISSUER 环境变量），回退到自建登录
// 否则直接跳转 /api/auth/login
```

- [ ] **Step 3: 提交**

```bash
git add src/providers/AuthProvider.tsx src/pages/Login.tsx
git commit -m "feat: adapt frontend auth flow for MaxKey OIDC"
```

---

## Task 15: 空间管理员权限

**Files:**
- Modify: `server/db/schema.ts` — 新增 `spaceMembers` 表
- Create: `server/db/migrations/0003_space_members.sql`
- Modify: `server/lib/permissions.ts` — 加空间管理员检查

- [ ] **Step 1: 新增 space_members 表和 role 枚举扩展**

```typescript
// server/db/schema.ts
export const spaceMembers = pgTable("space_members", {
  id: uuid("id").defaultRandom().primaryKey(),
  spaceId: uuid("space_id").notNull().references(() => knowledgeSpaces.id, { onDelete: "cascade" }),
  userId: uuid("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
  role: varchar("role", { length: 20 }).notNull().default("member"), // "admin" | "member"
  createdAt: timestamp("created_at").defaultNow().notNull(),
}, (t) => [
  index("idx_space_members_space").on(t.spaceId),
  index("idx_space_members_user").on(t.userId),
]);
```

- [ ] **Step 2: 创建 migration**

```sql
-- server/db/migrations/0003_space_members.sql
CREATE TABLE space_members (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  space_id UUID NOT NULL REFERENCES knowledge_spaces(id) ON DELETE CASCADE,
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  role VARCHAR(20) NOT NULL DEFAULT 'member',
  created_at TIMESTAMP DEFAULT NOW()
);
CREATE INDEX idx_space_members_space ON space_members(space_id);
CREATE INDEX idx_space_members_user ON space_members(user_id);
```

- [ ] **Step 3: 更新权限逻辑**

```typescript
// server/lib/permissions.ts — canEdit / canRead 中增加空间管理员判断
export async function canEdit(user, obj) {
  if (!user) return false;
  if (user.role === "admin") return true;
  if (obj.ownerId === user.id) return true;

  // 检查是否是空间管理员
  const member = await db.select().from(spaceMembers)
    .where(and(eq(spaceMembers.userId, user.id), eq(spaceMembers.spaceId, obj.spaceId), eq(spaceMembers.role, "admin")))
    .limit(1);
  if (member.length > 0) return true;

  // ... 其余逻辑不变
}
```

- [ ] **Step 4: 提交**

```bash
git add server/db/schema.ts server/db/migrations/0003_space_members.sql server/lib/permissions.ts
git commit -m "feat: add space_members table and space admin permission logic"
```

---

## Task 16: 版本控制补齐

**Files:**
- Modify: `server/db/schema.ts` — knowledgeObjects 加 `changeNote` 字段
- Create: `server/db/migrations/0004_change_note.sql`
- Create: `server/db/migrations/0005_knowledge_file_versions.sql`
- Modify: `server/routers/knowledge.ts` — 回滚接口

- [ ] **Step 1: 添加 changeNote 字段**

```typescript
// server/db/schema.ts — knowledgeObjects 表中新增
changeNote: text("change_note"),
```

```sql
-- server/db/migrations/0004_change_note.sql
ALTER TABLE knowledge_objects ADD COLUMN change_note TEXT;
```

- [ ] **Step 2: 创建 knowledge_file_versions 表**

```typescript
// server/db/schema.ts
export const knowledgeFileVersions = pgTable("knowledge_file_versions", {
  id: uuid("id").defaultRandom().primaryKey(),
  objectId: uuid("object_id").notNull().references(() => knowledgeObjects.id, { onDelete: "cascade" }),
  version: integer("version").notNull(),
  filePath: text("file_path").notNull(),    // Nextcloud 路径
  fileSize: integer("file_size"),
  fileHash: varchar("file_hash", { length: 128 }),
  fileName: varchar("file_name", { length: 500 }),
  fileType: varchar("file_type", { length: 100 }),
  uploadedAt: timestamp("uploaded_at").defaultNow().notNull(),
}, (t) => [
  index("idx_kfv_object").on(t.objectId),
]);
```

- [ ] **Step 3: 添加回滚接口**

```typescript
// server/routers/knowledge.ts — 新增 POST /knowledge/:id/rollback/:versionId
router.post("/knowledge/:id/rollback/:versionId", requireAuth, requireEdit, async (req, res) => {
  // 1. 将当前已发布版本归档
  // 2. 将目标历史版本状态改为 published
  // 3. 版本号设为历史版本号 + 1
  // 4. 返回新版本
});
```

- [ ] **Step 4: 提交**

```bash
git add server/db/schema.ts server/db/migrations/ server/routers/knowledge.ts
git commit -m "feat: add change_note, knowledge_file_versions table, and rollback endpoint"
```

---

## Task 17: 文件预览后端（LibreOffice）

**Files:**
- Create: `server/lib/preview.ts`
- Create: `server/routers/preview.ts`
- Modify: `server/index.ts` — 注册 preview 路由

- [ ] **Step 1: 创建预览转换逻辑**

```typescript
// server/lib/preview.ts
import { exec } from "child_process";
import { promisify } from "util";
import path from "path";
import fs from "fs";

const execAsync = promisify(exec);
const CACHE_DIR = "/tmp/knowledge-previews";

// 确保缓存目录存在
fs.mkdirSync(CACHE_DIR, { recursive: true });

export async function convertOfficeToPdf(filePath: string): Promise<string> {
  const outputDir = path.join(CACHE_DIR, Buffer.from(filePath).toString("base64").slice(0, 32));
  const baseName = path.basename(filePath, path.extname(filePath));
  const pdfPath = path.join(outputDir, baseName + ".pdf");

  // 缓存命中
  if (fs.existsSync(pdfPath)) return pdfPath;

  fs.mkdirSync(outputDir, { recursive: true });
  await execAsync(`libreoffice --headless --convert-to pdf --outdir "${outputDir}" "${filePath}"`);
  return pdfPath;
}
```

- [ ] **Step 2: 创建预览路由**

```typescript
// server/routers/preview.ts
import { Router } from "express";
import { requireAuth } from "../middleware/auth";
import { convertOfficeToPdf } from "../lib/preview";
import path from "path";
import fs from "fs";

const router = Router();

// GET /api/preview/raw?path=... — 直接返回文件（PDF/图片/文本）
router.get("/preview/raw", requireAuth, async (req, res) => {
  const filePath = req.query.path as string;
  // 校验 filePath 指向 Nextcloud 缓存目录内，防止路径穿越
  // 设置 Content-Type，pipe 文件流
});

// GET /api/preview/office?path=... — Office 转 PDF 后返回
router.get("/preview/office", requireAuth, async (req, res) => {
  const filePath = req.query.path as string;
  const pdfPath = await convertOfficeToPdf(filePath);
  res.setHeader("Content-Type", "application/pdf");
  fs.createReadStream(pdfPath).pipe(res);
});
```

- [ ] **Step 3: 注册路由**

```typescript
// server/index.ts
import previewRouter from "./routers/preview";
app.use("/api", previewRouter);
```

- [ ] **Step 4: 提交**

```bash
git add server/lib/preview.ts server/routers/preview.ts server/index.ts
git commit -m "feat: add file preview backend with LibreOffice conversion"
```

---

## Task 18: 搜索功能启用

**Files:**
- Modify: `server/lib/search.ts`

- [ ] **Step 1: 检查 search_vector 功能是否已启用**

```bash
rg "search_vector\|searchQuery\|to_tsvector\|ts_rank" server/lib/search.ts
```

- [ ] **Step 2: 如未完整启用，补全搜索实现**

```typescript
// server/lib/search.ts
import { sql } from "drizzle-orm";

// 返回 PostgreSQL tsquery 条件
export function searchQuery(q: string) {
  const tsquery = q.trim().split(/\s+/).map(w => w + ":*").join(" & ");
  return sql`to_tsvector('simple', coalesce(${knowledgeObjects.title}, '') || ' ' || coalesce(${knowledgeObjects.description}, '')) @@ to_tsquery('simple', ${tsquery})`;
}
```

- [ ] **Step 3: 确认搜索结果按相关度排序**

在搜索路由中添加 `orderBy(desc(sql`ts_rank(...)`))`。

- [ ] **Step 4: 提交**

```bash
git add server/lib/search.ts
git commit -m "feat: enable PostgreSQL full-text search with relevance ranking"
```

---

## Task 19: 通知功能补齐

**Files:**
- Modify: `server/lib/notify.ts`

- [ ] **Step 1: 增强 notify.ts**

```typescript
// server/lib/notify.ts — 新增通知触发点
// 1. 文档状态变更时通知作者（审核通过/驳回）
// 2. 新文档发布时通知空间所有成员
// 3. 分配给审核员时通知审核员

export async function notifyReviewResult(objectId: string, action: "approved" | "rejected") {
  const [obj] = await db.select().from(knowledgeObjects).where(eq(knowledgeObjects.id, objectId)).limit(1);
  if (!obj) return;

  const type = action === "approved" ? "review_approved" : "review_rejected";
  await db.insert(notifications).values({
    userId: obj.ownerId,
    type,
    objectId,
    message: `你的文档「${obj.title}」已${action === "approved" ? "通过审核" : "被驳回"}`,
  });
}

export async function notifySpaceMembers(objectId: string) {
  // 查询空间所有成员，为每人创建通知
  const [obj] = await db.select().from(knowledgeObjects).where(eq(knowledgeObjects.id, objectId)).limit(1);
  if (!obj) return;

  const members = await db.select().from(spaceMembers).where(eq(spaceMembers.spaceId, obj.spaceId));
  for (const m of members) {
    await db.insert(notifications).values({
      userId: m.userId,
      type: "new_version",
      objectId,
      message: `空间有新内容发布：「${obj.title}」`,
    });
  }
}
```

- [ ] **Step 2: 创建通知 API 路由**

```typescript
// server/routers/notifications.ts（如不存在则创建）
// GET /api/notifications — 获取当前用户通知列表
// POST /api/notifications/:id/read — 标记单条已读
// POST /api/notifications/read-all — 标记全部已读
```

- [ ] **Step 3: 集成 AuthProvider 获取未读计数**

```typescript
// src/providers/AuthProvider.tsx — 在 checkAuth 后查询 /api/notifications/count
```

- [ ] **Step 4: 提交**

```bash
git add server/lib/notify.ts server/routers/notifications.ts src/providers/AuthProvider.tsx
git commit -m "feat: complete notification system with bell count and notification center"
```

---

## Task 20: E2E 测试更新

**Files:**
- Modify: `e2e/login.spec.ts` — 适配 OIDC 流程
- Modify: 其它受影响的 spec 文件
- Create: `e2e/notifications.spec.ts` — 新通知功能测试

- [ ] **Step 1: 运行现有测试，看哪些因 UI 变化而失败**

```bash
npx playwright test --reporter=list 2>&1 | head -50
```

- [ ] **Step 2: 逐个修复失败的测试**

主要问题预计是：
- `login.spec.ts` — 认证流程变了
- 页面选择器变化（class 名改了）
- 导航结构变了

- [ ] **Step 3: 添加新功能测试**

```typescript
// e2e/notifications.spec.ts
test("notification bell shows unread count", async ({ page }) => { ... });
test("notification center displays list", async ({ page }) => { ... });
```

- [ ] **Step 4: 运行全量测试确保通过**

```bash
npx playwright test
```

- [ ] **Step 5: 提交**

```bash
git add e2e/
git commit -m "test: update E2E tests for new UI and auth flow"
```

---

## Task 21: 部署脚本更新

**Files:**
- Modify: `scripts/deploy.sh`
- Modify: `.env.example`

- [ ] **Step 1: 更新 .env.example**

```env
# 已有变量保持不变
# 新增:
MAXKEY_ISSUER=https://maxkey.your-domain.com
MAXKEY_CLIENT_ID=knowledge-platform
MAXKEY_CLIENT_SECRET=change-me
MAXKEY_REDIRECT_URI=https://your-domain.com/api/auth/callback
```

- [ ] **Step 2: 更新 deploy.sh 确保安装 LibreOffice**

```bash
# scripts/deploy.sh — 在依赖安装步骤后追加:
sudo apt-get install -y libreoffice-core libreoffice-writer libreoffice-calc libreoffice-impress
mkdir -p /tmp/knowledge-previews
```

- [ ] **Step 3: 确认 pm2 配置**

```bash
# 确保 pm2 start 命令正确
pm2 start dist/index.js --name knowledge-platform --node-args="--experimental-specifier-resolution=node"
```

- [ ] **Step 4: 提交**

```bash
git add scripts/deploy.sh .env.example
git commit -m "chore: update deploy scripts for LibreOffice and MaxKey config"
```

---

## 执行顺序

```
Task 1   cn() 工具函数           ← 无依赖
Task 2   设计 Token 与字体        ← 无依赖
Task 3   导航布局                ← 依赖 Task 1, 2
Task 4   统计卡片                ← 依赖 Task 1
Task 5   工作台                  ← 依赖 Task 3, 4
Task 6   文档列表                ← 依赖 Task 3
Task 7   用户管理                ← 依赖 Task 3
Task 8   部门管理 + 空间管理      ← 依赖 Task 3
Task 9   通知中心                ← 依赖 Task 3
Task 10  文件预览弹窗            ← 依赖 Task 3
Task 11  文档详情                ← 依赖 Task 3, 10
Task 12  Wiki 富文本编辑器       ← 依赖 Task 3
Task 13  MaxKey OIDC（后端）     ← 依赖 Task 3（路由），与前端并行
Task 14  前端 OIDC 适配          ← 依赖 Task 13
Task 15  空间管理员权限          ← 依赖 Task 13
Task 16  版本控制补齐            ← 可并行
Task 17  文件预览后端            ← 可并行
Task 18  搜索启用                ← 可并行
Task 19  通知后端                ← 依赖 Task 15
Task 20  E2E 测试                ← 依赖所有功能任务
Task 21  部署脚本                ← 最后
```

---

**计划完成。** 共 21 个任务，覆盖设计文档全部 10 个模块。每个任务都可独立开发和测试。
